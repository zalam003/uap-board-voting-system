# UAP Board Voting System - Docker Environment Configuration
# Copy this file to .env for Docker Compose

# Database Configuration
DB_PASSWORD=uap_voting_secure_password
DB_PORT=5432

# Application Configuration
NODE_ENV=production
APP_PORT=3000
FRONTEND_URL=http://localhost:3000

# Security Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-make-it-very-long-and-random
ADMIN_SECRET=your-admin-secret-password-change-this

# Gmail SMTP Configuration (Required for email functionality)
GMAIL_USER=your-gmail-address@gmail.com
GMAIL_APP_PASSWORD=your-gmail-app-password-here
FROM_EMAIL=your-gmail-address@gmail.com

# pgAdmin Configuration (Optional - for database management)
PGADMIN_EMAIL=admin@uap-bd.edu
PGADMIN_PASSWORD=admin_password
PGADMIN_PORT=5050

# Build Configuration
BUILD_TARGET=production

# Development Overrides (uncomment for development)
# NODE_ENV=development
# BUILD_TARGET=development
# DB_PASSWORD=dev_password
# JWT_SECRET=dev-jwt-secret-not-for-production
# ADMIN_SECRET=dev-admin-password
